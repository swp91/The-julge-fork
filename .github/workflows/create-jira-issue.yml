name: Create Jira Issue with Assignee

on:
  issues:
    types:
      - opened

jobs:
  create-jira-issue:
    name: Create Jira Issue
    runs-on: ubuntu-latest
    steps:
      - name: Login to Jira
        uses: atlassian/gajira-login@v3
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}

      - name: Create JSON Mapping File
        run: |
          echo '{
            "Stemplun": "712020:69408110-5a5a-40db-950d-da7d72f75f1a",
            "jh-zhirun": "712020:69ecba30-a6f7-4239-bb86-706266c54879",
            "Hogn-hyeonbin": "712020:9490ee23-f329-4af1-929d-82c1ae0902ff",
            "swp91": "712020:2028a842-679b-4fc1-a0e2-83b3bf158ef1"
          }' > mapping.json
          echo "Mapping file created."

      - name: Get Assignee for Jira
        id: get-assignee
        run: |
          # GitHub Issue 작성자 확인
          GITHUB_USERNAME="${{ github.event.issue.user.login }}"
          echo "GitHub Username: $GITHUB_USERNAME"

          # JSON 파일에서 Assignee 추출
          ASSIGNEE=$(jq -r --arg USERNAME "$GITHUB_USERNAME" '.[$USERNAME]' mapping.json)
          echo "Parsed Assignee: $ASSIGNEE"

          if [ "$ASSIGNEE" == "null" ]; then
            echo "Error: No Jira user mapping found for GitHub user: $GITHUB_USERNAME"
            exit 1
          fi

          echo "Jira Assignee: $ASSIGNEE"
          echo "ASSIGNEE=$ASSIGNEE" >> $GITHUB_ENV

      - name: Create Jira Issue
        id: create-jira
        uses: atlassian/gajira-create@v3
        with:
          project: T3
          issuetype: Task
          summary: "${{ github.event.issue.title }}"
          description: "${{ github.event.issue.body }}"
          fields: |
            {
              "assignee": {
                "accountId": "${{ env.ASSIGNEE }}"
              }
            }

      - name: Add Jira Issue Key to GitHub Issue
        uses: actions/github-script@v6
        with:
          script: |
            const jiraIssueKey = '${{ steps.create-jira.outputs.issue }}';
            const issueNumber = context.payload.issue.number;

            // GitHub Issue에 Jira Issue 키를 댓글로 추가
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: `Jira Issue created: ${jiraIssueKey}`
            });
